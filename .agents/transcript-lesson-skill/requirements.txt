# Install into an isolated venv only — never globally:
#   uv venv && uv pip install -r requirements.txt
# Run via: uv run python scripts/extract_transcript.py "<url>"
youtube-transcript-api>=0.6.2,<2
requests>=2.31,<3
beautifulsoup4>=4.12,<5
yt-dlp>=2023.12,<2027
